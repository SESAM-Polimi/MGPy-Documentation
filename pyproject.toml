[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "MGPy-Documentation"
authors = [{name = "Alessandro Onori", email = "alessandro.onori@mail.polimi.it"}]
dynamic = ["version", "description"]
